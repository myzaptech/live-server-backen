# ==================== CONFIGURACIÓN DE PRODUCCIÓN ====================
# Copia este archivo y ajusta los valores según tu servidor de DigitalOcean

# Puerto de la API (usualmente 5000, Nginx hará el proxy)
PORT=5000

# Puerto RTMP para recibir streams de OBS
RTMP_PORT=1935

# Puerto HTTP para servir archivos HLS
HTTP_PORT=8000

# Dominio o IP de tu droplet (SIN http:// ni https://)
# Ejemplo: streaming.midominio.com o 192.168.1.100
DOMAIN=myzaptech.site

# Stream key (cambia esto por algo seguro)
STREAM_KEY=live

# Path de FFmpeg en el servidor (Ubuntu/Debian)
FFMPEG_PATH=/usr/bin/ffmpeg

# Directorio para archivos HLS (asegúrate que Nginx tenga acceso)
HLS_PATH=/var/www/streams

# Configuración CORS
# * = permite todos los orígenes (recomendado para streaming público)
# o especifica dominios separados por comas: https://myzaptech.site,https://www.myzaptech.site
CORS_ORIGIN=*

# NODE_ENV
NODE_ENV=production
